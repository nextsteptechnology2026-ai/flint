Pendientes de Flint

Lo terminado no se anota acá: está en el historial de git.

Medianos:
1. API de plugins. Falta:
   - definir un lenguaje nuevo con su resaltado;
   - correr algo ante un evento (guardar, abrir), no solo por comando.

Chicos:
2. Revisar la próxima release, que es la primera con estos cambios:
   - que el .deb del runner sea idéntico a uno armado con
     packaging/build-deb.sh dentro de Ubuntu 24.04 (en local ya da lo
     mismo en rutas distintas; entre máquinas falta verlo);
   - que salgan el .deb y el tar.gz de Linux ARM (runner
     ubuntu-24.04-arm), que no se pudieron probar acá.

Por decidir:
- Si el CI corre `cargo fmt --check`. Nadie comprobó si el código pasa
  rustfmt; activarlo sin revisar puede obligar a reformatear todo.

Descartado a propósito (no proponer de nuevo sin un motivo nuevo):
- Terminal integrada: tmux ya lo resuelve.
- Paneles divididos: las pestañas y tmux alcanzan. Si algún día se hace,
  el scroll tiene que pasar del Editor a una struct de vista.
- Gestor de plugins: no hace falta mientras haya pocos plugins; se copian
  a ~/.config/flint/plugins/.
- Los ~130 lenguajes de micro: hay 17 con tree-sitter. Agregar uno es una
  fila en LENGUAJES (highlight.rs) y una variante de Lang.
- Leer el portapapeles por OSC 52: las terminales lo traen apagado por
  seguridad.
